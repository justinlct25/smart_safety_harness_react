export const largeScreen =
  window.innerWidth > 1400 || window.innerHeight > 1120;
export const isDesktop = window.innerWidth < 1400 || window.innerHeight < 1120;
export const isTablet = window.innerWidth < 1025 || window.innerHeight < 892;
export const isMobile = window.innerWidth < 760 || window.innerHeight < 730;
